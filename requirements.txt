# Agentic AI Lifecycle Systems — dependencies for the book's code blueprints
# Install with:  pip install -r requirements.txt

# Data validation / structured outputs (Phase 4)
pydantic>=2.6

# PostgreSQL access — checkpoints, intent log, idempotency (Phases 2-4)
psycopg2-binary>=2.9

# Redis — high-availability state & agent status (Phase 4.5)
redis>=5.0

# HTTP calls to payment / LLM APIs (Phase 3)
requests>=2.31

# Web layer for human-in-the-loop endpoints (Phase 4.3)
fastapi>=0.110
uvicorn>=0.29

# Numerics — semantic caching / similarity (Phase 2.5)
numpy>=1.26

# Distributed tracing & forensic telemetry (Phase 4.2)
opentelemetry-api>=1.24
opentelemetry-sdk>=1.24
openinference-semantic-conventions>=0.1.9
